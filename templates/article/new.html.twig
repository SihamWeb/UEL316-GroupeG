{% extends 'base.html.twig' %}

{% block title %}Nouvel article - ActuG{% endblock %}

{% block body %}
    
    {% if app.user %}
    <h1 class="font-weight-normal text-center mb-4">Créer un nouvel article</h1>

    <div class="container">
        <div class="row justify-content-center mt-5">
            <div class="col-md-6">
                {{ include('article/_form.html.twig', {'button_label': 'Créer'}) }}
            </div>
        </div>
    </div>
    {% endif %}
{% endblock %}
